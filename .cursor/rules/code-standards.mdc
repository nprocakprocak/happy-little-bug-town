---
description: Core TypeScript and React coding standards
alwaysApply: true
---

# Core Coding Standards

- Use TypeScript strictly (no `any`).
- Prefer functional React components.
- Keep components small and single-responsibility.
- Avoid components over ~150 lines.
- Prefer interface over type when defining component props.
- Use clear naming (PascalCase for components, camelCase for variables, UPPER_SNAKE_CASE for constants).
- Avoid using z-index in Html/Css and corresponding Tailwind classes.
- Avoid deeply nested logic.
- Write clean, readable, self-documenting code.
- Don't use comments at all if not asked by user but don't remove existing comments.
- After every work on frontend, check the code with prettier (npm run format:check).
- Don't use "readonly" keyword.
- Don't use then() or catch() when working with Promises unless you can't avoid it. Prefer async methods even in callbacks.
